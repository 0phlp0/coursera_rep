browser-sync start --server --directory --files "*" <- das funktioniert nur für Datein in EINEM Ordner

browser-sync start --server --directory --files "**/*" <- das funktioniert für alle Dateien in EINEM Ordner und für alle Unterordner
